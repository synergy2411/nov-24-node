const sum = (n1, n2) => n1 + n2;
const MAGIC_NUMBER = Math.round(Math.random() * 100);

const getMyLuckyNumber = () => MAGIC_NUMBER;

module.exports = { sum, getMyLuckyNumber };

// Encapsulation / Abstraction
